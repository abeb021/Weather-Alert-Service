events {
    #how many connections to handle at a time 
    worker_connections 1024;
}
http {
    upstream auth_service {
        server auth-service:8081;
    }

    server {
        listen 80;

        location /api/login {
            proxy_pass http://auth_service;
        }
    }
}